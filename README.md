# Cheat Sheet TypeScript

## Lancer rapidement
- `npm run play` : exécuter `src/99-playground/Playground.ts`
- `npm run watch` : compiler en continu vers `dist/`
- `npm run start` : exécuter `dist/index.js`
